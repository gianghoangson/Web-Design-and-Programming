%% ── Theme colour aliases (th* → dlep* bridge) ───────────────────────────────
%% Add this block to your preamble AFTER the \definecolor{dlep*} definitions.
%% All skill-generated frame code uses th* names; these aliases resolve them.
%%
\colorlet{thBlue}       {dlepBlue}
\colorlet{thAccent}     {dlepAccent}
\colorlet{thLight}      {dlepLight}
\colorlet{thGrey}       {dlepGrey}
\colorlet{thOrange}     {dlepOrange}
\colorlet{thGreen}      {dlepGreen}
\colorlet{thDeepOrange} {dlepDeepOrange}
